version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ../nacho-banana:/app
    ports:
      - '3200:3200'
    networks:
      - integration_default

networks:
  integration_default:
    external: true
